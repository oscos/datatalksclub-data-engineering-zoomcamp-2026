 I am using this file as demo only.
# Since `.env` file should not be commited to git, 
# as it should be added to `gitignore`.
# You will want to update the variable values  below to your values
# and save this file as `.env`. 

# ----------------------------
# PostgreSQL
# ----------------------------
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_DB=ny_taxi

# ----------------------------
# pgAdmin
# ----------------------------
PGADMIN_EMAIL=pgadmin@pgadmin.com
PGADMIN_PASSWORD=root

